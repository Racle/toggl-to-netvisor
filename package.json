{
  "name": "toggl-to-netvisor",
  "version": "1.0.0",
  "description": "Convert Toggl time entries to Netvisor-ready hours with CSRF protection",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.0.1",
    "express-rate-limit": "^8.1.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
